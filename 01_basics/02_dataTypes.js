"use strict" // trate all JS code as newer version

// alert("hoi") // we are using nodejs not browser
let name="dev" //string data types 
let age =21 // integer 
let flag= true //boolean 

// number(2^53) , bigint,string, boolean,null,undefine  , symbol,object

console.log(typeof(true))